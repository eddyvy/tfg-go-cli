version: 0.0.1
project:
  base: github.com/eddyvy/
  name: tennis
database:
  type: postgresql
  tables:
    - name: user
      columns:
        - name: id
          type: integer
          primary_key: true
        - name: name
          type: string
        - name: email
          type: string
        - name: password
          type: string
    - name: post
      columns:
        - name: id
          type: integer
          primary_key: true
        - name: title
          type: string
        - name: content
          type: string
        - name: user_id
          type: integer
